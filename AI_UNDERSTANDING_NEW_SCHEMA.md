# Как AI будет понимать Redis → Context7 в новой схеме

## 🎯 Вопрос

Как AI будет изначально понимать, что если она работает с Redis, то можно вызвать MCP сервер Context7, чтобы получить точную документацию, и всё это надо будет сделать через docker-mcp-orchestrator?

## ✅ Ответ: Многоуровневая система понимания

### Уровень 1: Краткий каталог показывает все возможности

```
AI вызывает: list_available_servers()

Получает:
📦 Available MCP Servers (12)

🗄️ Database
  • redis (⚪) - Redis cache and data store
    ~15 tools

📚 Documentation
  • context7 (⚪) - Library documentation
    ~2 tools

AI видит:
✅ Есть redis для работы с Redis
✅ Есть context7 для документации
✅ AI понимает что можно использовать оба
```

### Уровень 2: Явные инструкции в docstrings

```python
@mcp.tool()
async def activate_server(server_name: str, reason: str = "") -> str:
    """
    Activate (enable) an MCP server.
    
    **CRITICAL AI INSTRUCTIONS:**
    
    When working with ANY library/framework (Redis, PostgreSQL, etc.):
    
    1. Activate the library server (e.g., "redis")
    2. Context7 is automatically activated for documentation
    3. Use context7 tools to get library docs:
       - resolve-library-id("library_name")
       - get-library-docs(library_id, query="...")
    4. Then use library tools
    
    Example:
    User: "Set up Redis cache"
    → activate_server("redis")
    → Context7 auto-activated
    → Use context7 for Redis docs
    → Use redis tools
    """
```

**AI читает docstring и понимает:**
- ✅ Для библиотек нужно активировать сервер
- ✅ Context7 активируется автоматически
- ✅ Нужно использовать context7 для документации

### Уровень 3: Автоматическая активация context7

```python
# При активации redis автоматически активируется context7
activate_server("redis")
→ redis активирован
→ context7 автоматически активирован (для документации)
→ AI получает оба сервера готовыми
```

**AI не нужно думать** - context7 активируется автоматически!

### Уровень 4: Специальный инструмент для библиотек

```python
@mcp.tool()
async def setup_library_environment(library_name: str) -> str:
    """
    Smart setup for working with a library.
    
    Automatically activates:
    1. The library server (redis, postgres, etc.)
    2. Context7 server for documentation
    
    Example:
    setup_library_environment("redis")
    → Activates redis + context7
    → Returns instructions how to use both
    """
```

**AI может использовать:**
- `setup_library_environment("redis")` - проще и понятнее
- Или `activate_server("redis")` - context7 активируется автоматически

### Уровень 5: Умные рекомендации

```python
@mcp.tool()
async def activate_for_task(task_description: str) -> str:
    """
    Auto-detect and activate servers for a task.
    
    Example:
    activate_for_task("set up redis cache")
    → Detects: redis (keyword "redis")
    → Auto-adds: context7 (for documentation)
    → Activates both
    """
```

**AI может использовать:**
- `activate_for_task("redis cache")` - автоматически активирует redis + context7

## 🔄 Полный Workflow для AI

### Сценарий: "Настрой Redis кэш"

#### Вариант 1: Использование setup_library_environment (рекомендуется)

```
1. Пользователь: "Настрой Redis кэш"

2. AI видит в каталоге:
   • redis - для работы с Redis
   • context7 - для документации

3. AI вызывает: setup_library_environment("redis")
   
   Orchestrator:
   ✅ Активирует redis
   ✅ Активирует context7
   ✅ Возвращает инструкции

4. AI получает:
   ✅ redis activated
   ✅ context7 activated for documentation
   
   Next steps:
   1. Get documentation:
      - resolve-library-id("redis")
      - get-library-docs(redis_id, query="cache setup")
   2. Use Redis tools:
      - redis_set(...)
      - redis_config_set(...)

5. AI выполняет:
   a) resolve-library-id("redis") → library_id
   b) get-library-docs(redis_id, query="cache setup for sessions")
   c) Читает документацию
   d) Использует redis tools правильно
```

#### Вариант 2: Использование activate_server (автоматическая активация)

```
1. Пользователь: "Настрой Redis кэш"

2. AI вызывает: activate_server("redis")
   
   Orchestrator:
   ✅ Активирует redis
   ✅ Автоматически активирует context7 (зависимость)
   ✅ Возвращает tools обоих серверов

3. AI получает:
   ✅ redis activated
   ✅ context7 auto-activated for documentation
   
   Available tools:
   • Redis tools (15)
   • Context7 tools (2)
     - resolve-library-id("redis")
     - get-library-docs(...)

4. AI использует оба сервера
```

#### Вариант 3: Использование activate_for_task (умный выбор)

```
1. Пользователь: "Настрой Redis кэш"

2. AI вызывает: activate_for_task("set up redis cache")
   
   Orchestrator:
   ✅ Находит "redis" по ключевому слову
   ✅ Автоматически добавляет "context7" (для документации)
   ✅ Активирует оба

3. AI получает:
   ✅ redis activated
   ✅ context7 activated (auto-added for documentation)

4. AI использует оба сервера
```

## 🧠 Как AI понимает связи

### 1. Через каталог

```
AI видит в каталоге:
• redis - для работы
• context7 - для документации

AI понимает: можно использовать оба вместе
```

### 2. Через docstrings

```
AI читает docstring activate_server():
"Context7 is automatically activated for documentation"

AI понимает: context7 активируется автоматически
```

### 3. Через автоматическую активацию

```
AI вызывает: activate_server("redis")
Получает: redis + context7 активированы

AI понимает: context7 уже готов, можно использовать
```

### 4. Через специальный инструмент

```
AI видит: setup_library_environment("redis")
Понимает: это активирует redis + context7 вместе
```

### 5. Через умные рекомендации

```
AI вызывает: activate_for_task("redis")
Получает: redis + context7 (автоматически)

AI понимает: orchestrator сам добавил context7
```

## 📋 Чеклист для AI

### Когда пользователь просит работать с библиотекой:

1. ✅ **AI видит каталог** - знает что есть redis и context7
2. ✅ **AI выбирает метод:**
   - `setup_library_environment("redis")` - лучший вариант
   - `activate_for_task("redis")` - умный выбор
   - `activate_server("redis")` - context7 активируется автоматически
3. ✅ **AI получает оба сервера** - redis + context7
4. ✅ **AI использует context7** для получения документации
5. ✅ **AI использует redis tools** для работы

## 🎯 Ключевые моменты

### 1. AI не должна думать о зависимостях

```
❌ Старое: AI должна сама понять что нужен context7
✅ Новое: context7 активируется автоматически
```

### 2. AI видит все возможности в каталоге

```
✅ AI видит: redis, context7, и другие серверы
✅ AI понимает: можно использовать вместе
```

### 3. Явные инструкции в docstrings

```
✅ AI читает: "Context7 auto-activated for documentation"
✅ AI понимает: не нужно активировать вручную
```

### 4. Специальный инструмент упрощает

```
✅ setup_library_environment("redis")
✅ Один вызов → оба сервера активированы
```

## ✅ Итоговый ответ

**AI понимает про Redis → Context7 через:**

1. **Каталог** - видит оба сервера доступными
2. **Docstrings** - читает инструкции про автоматическую активацию
3. **Автоматическая активация** - context7 активируется сам
4. **Специальный инструмент** - setup_library_environment() упрощает
5. **Умные рекомендации** - activate_for_task() добавляет context7 автоматически

**AI не нужно понимать архитектуру** - всё происходит автоматически или через явные инструкции!

---

## 🔄 Пример полного диалога

```
Пользователь: "Настрой Redis кэш для сессий"

AI:
1. Вызывает list_available_servers()
   → Видит: redis, context7 доступны

2. Вызывает setup_library_environment("redis")
   → Получает: redis + context7 активированы
   → Получает инструкции

3. Использует context7:
   → resolve-library-id("redis")
   → get-library-docs(redis_id, query="session cache")

4. Читает документацию про Redis sessions

5. Использует redis tools:
   → redis_config_set("maxmemory", "256mb")
   → redis_set("session:user123", "data")
   → redis_expire("session:user123", 3600)

✅ Всё работает правильно благодаря документации!
```

**AI понимает связи через каталог, инструкции и автоматизацию!** 🎉
