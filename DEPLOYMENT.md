# Инструкции по развертыванию Docker MCP Orchestrator

## 📋 Предварительные требования

1. **Python 3.11+** установлен
2. **Docker Desktop** с установленным **MCP Toolkit**
3. **MCP серверы** добавлены в Docker MCP Catalog

## 🚀 Установка

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

Или в режиме разработки:

```bash
pip install -e .
```

### Шаг 2: Проверка конфигурации

Убедитесь, что файл `config/config.yaml` существует и настроен правильно.

### Шаг 3: Проверка Docker MCP Toolkit

Убедитесь, что Docker MCP Toolkit доступен:

```bash
docker mcp --version
docker mcp catalog ls
```

### Шаг 4: Добавление в Cursor

#### Вариант A: Через конфигурационный файл

Создайте или отредактируйте файл конфигурации Cursor для MCP серверов.

**Для macOS/Linux**: `~/.cursor/mcp.json` или `~/.config/cursor/mcp.json`
**Для Windows**: `%APPDATA%\Cursor\mcp.json`

```json
{
  "mcpServers": {
    "docker-mcp-orchestrator": {
      "command": "python",
      "args": [
        "-m",
        "orchestrator"
      ],
      "cwd": "/absolute/path/to/docker-mcp-orchestrator"
    }
  }
}
```

Или с абсолютным путем к скрипту:

```json
{
  "mcpServers": {
    "docker-mcp-orchestrator": {
      "command": "python",
      "args": [
        "/absolute/path/to/docker-mcp-orchestrator/src/orchestrator/__main__.py"
      ]
    }
  }
}
```

#### Вариант B: Через настройки Cursor

1. Откройте настройки Cursor
2. Найдите раздел MCP Servers
3. Добавьте новый сервер:
   - Name: `docker-mcp-orchestrator`
   - Command: `python`
   - Args: `["-m", "orchestrator"]` или путь к `__main__.py`
   - Working Directory: путь к проекту

### Шаг 5: Перезапуск Cursor

Перезапустите Cursor для загрузки нового MCP сервера.

### Шаг 6: Проверка работы

В Cursor попробуйте использовать tool `list_installed_servers` для проверки работы Orchestrator.

## 🔧 Настройка

### Конфигурация (config/config.yaml)

Основные настройки:

```yaml
orchestrator:
  cache:
    servers_ttl: 300  # TTL для кэша серверов (секунды)
    tools_ttl: 600    # TTL для кэша tools (секунды)
  
  docker_mcp:
    catalog: "docker-mcp"  # Имя каталога по умолчанию
    command_timeout: 30     # Таймаут команд (секунды)
    
  proxy:
    connection_timeout: 30  # Таймаут соединения (секунды)
    reconnect_attempts: 3  # Количество попыток переподключения
```

### Добавление AI правил

Добавьте правила из `prompts/ai_rules.md` в `.cursorrules` или аналогичный файл для правильной работы AI с Orchestrator.

## 🐛 Устранение неполадок

### Проблема: Orchestrator не запускается

**Решение**:
1. Проверьте, что Python 3.11+ установлен: `python --version`
2. Проверьте установку зависимостей: `pip list | grep mcp`
3. Проверьте путь к скрипту в конфигурации Cursor
4. Проверьте логи Cursor на наличие ошибок

### Проблема: Не работает Docker MCP Toolkit

**Решение**:
1. Убедитесь, что Docker Desktop запущен
2. Проверьте установку MCP Toolkit: `docker mcp --version`
3. Проверьте доступность каталога: `docker mcp catalog ls`

### Проблема: Tools не вызываются

**Решение**:
1. Убедитесь, что серверы запущены: используйте `get_active_servers`
2. Проверьте, что tools зарегистрированы: используйте `list_active_tools`
3. Используйте `call_tool` для вызова tools (не напрямую!)

### Проблема: Connection Pool не работает

**Решение**:
- Это известная проблема - механизм подключения к запущенным серверам требует реализации
- См. раздел "Известные проблемы" в README.md

## 📝 Проверка работы

### Тест 1: Список серверов

```python
# В Cursor попробуйте:
list_installed_servers()
```

Должен вернуться список установленных серверов.

### Тест 2: Запуск сервера

```python
# В Cursor попробуйте:
start_servers(servers=["github-mcp"])  # Замените на реальный сервер
```

Должен запуститься сервер и вернуться список tools.

### Тест 3: Вызов tool

```python
# В Cursor попробуйте:
call_tool(tool_name="some_tool", arguments={})
```

Должен выполниться tool через proxy.

## 🔄 Обновление

Для обновления Orchestrator:

```bash
git pull
pip install -r requirements.txt --upgrade
```

Перезапустите Cursor после обновления.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи Cursor
2. Проверьте логи Orchestrator (если доступны)
3. Убедитесь, что все зависимости установлены
4. Проверьте конфигурацию
