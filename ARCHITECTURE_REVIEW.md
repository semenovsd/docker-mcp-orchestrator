# Проверка архитектуры на соответствие возможностям Cursor, MCP и Docker

## 🔍 Анализ возможностей и ограничений

### 1. Cursor 2.x и MCP Protocol

#### Возможности:
- ✅ Cursor поддерживает подключение к MCP серверам через stdio
- ✅ Cursor может работать с Docker MCP Toolkit как с MCP сервером
- ✅ Cursor обнаруживает tools при установлении соединения

#### Ограничения:
- ⚠️ Cursor 2.x требует перезапуска соединения для обнаружения новых tools после активации сервера
- ⚠️ Нельзя динамически добавлять tools без перезапуска соединения

#### Вывод:
**Архитектура должна учитывать это ограничение**. Есть два подхода:

**Подход A: Управление через Docker MCP Toolkit Gateway (РЕКОМЕНДУЕТСЯ)**
- Все серверы подключаются к Docker MCP Toolkit gateway
- Cursor подключается к gateway один раз
- Orchestrator управляет видимостью tools через `docker mcp tools enable/disable`
- **Преимущество**: Не требуется перезапуск соединения Cursor
- **Недостаток**: Все tools всех серверов должны быть известны gateway (но могут быть отключены)

**Подход B: Управление серверами через enable/disable**
- Orchestrator активирует/деактивирует серверы через `docker mcp server enable/disable`
- При активации нового сервера требуется перезапуск соединения Cursor
- **Преимущество**: Полный контроль над активными серверами
- **Недостаток**: Требуется перезапуск соединения

**Рекомендация**: Использовать **комбинированный подход**:
1. Все серверы из Catalog подключаются к Docker MCP Toolkit gateway (но tools отключены по умолчанию)
2. Orchestrator включает нужные tools через `docker mcp tools enable`
3. Если требуется полная активация нового сервера, используется `docker mcp server enable` + уведомление о необходимости перезапуска

---

### 2. Docker MCP Toolkit Gateway

#### Возможности:
- ✅ Gateway может управлять множеством MCP серверов одновременно
- ✅ Gateway предоставляет единую точку подключения для клиентов
- ✅ Команды `docker mcp tools enable/disable` позволяют управлять видимостью tools
- ✅ Команды `docker mcp server enable/disable` позволяют управлять серверами
- ✅ Gateway может работать в режиме watch для автоматического обновления конфигурации

#### Команды для управления:
```bash
# Управление серверами
docker mcp server enable <server1> <server2> ...
docker mcp server disable <server1> <server2> ...

# Управление tools (более гранулярное)
docker mcp tools enable <tool1> <tool2> ...
docker mcp tools disable <tool1> <tool2> ...

# Получение информации
docker mcp server ls --json
docker mcp tools ls --format=json
docker mcp catalog show --format=json
```

#### Вывод:
**Docker MCP Toolkit предоставляет все необходимые возможности** для реализации Orchestrator:
- ✅ Управление серверами
- ✅ Управление tools
- ✅ Получение метаданных
- ✅ JSON API для парсинга

---

### 3. MCP Protocol

#### Возможности:
- ✅ MCP Protocol поддерживает stdio transport
- ✅ MCP Protocol поддерживает список tools через `tools/list`
- ✅ MCP Protocol поддерживает вызов tools через `tools/call`

#### Ограничения:
- ⚠️ MCP Protocol не поддерживает динамическое добавление tools без перезапуска соединения
- ⚠️ Список tools фиксируется при установлении соединения

#### Вывод:
**MCP Protocol ограничивает динамическое добавление tools**, но Docker MCP Toolkit Gateway решает эту проблему через механизм enable/disable tools.

---

### 4. Архитектурное решение

#### Рекомендуемая архитектура:

```
┌─────────────┐
│   Cursor    │
└──────┬──────┘
       │ MCP Protocol (stdio)
       │ Подключение один раз
       ▼
┌─────────────────────────────┐
│  Docker MCP Toolkit Gateway │
│  (docker mcp gateway run)   │
│                             │
│  Все серверы подключены,    │
│  но tools отключены по      │
│  умолчанию                  │
└──────┬──────────────────────┘
       │
       ├─► github-mcp (tools disabled)
       ├─► postgres-mcp (tools disabled)
       ├─► filesystem-mcp (tools disabled)
       └─► ... (все серверы из Catalog)

       │ CLI Commands
       ▼
┌─────────────────────────────┐
│  Orchestrator MCP Server    │
│  (docker-mcp-orchestrator)  │
│                             │
│  Управляет видимостью tools │
│  через docker mcp tools      │
│  enable/disable             │
└─────────────────────────────┘
```

#### Workflow:

1. **Инициализация**:
   - Docker MCP Toolkit Gateway запускается со всеми серверами из Catalog
   - Все tools отключены по умолчанию (или включены только базовые)
   - Cursor подключается к gateway один раз

2. **Работа AI**:
   - AI запрашивает список доступных серверов через Orchestrator
   - AI выбирает нужные серверы
   - AI запрашивает активацию tools через Orchestrator
   - Orchestrator включает нужные tools через `docker mcp tools enable`
   - Tools становятся доступны AI без перезапуска соединения

3. **Завершение**:
   - После завершения задачи AI запрашивает деактивацию tools
   - Orchestrator отключает tools через `docker mcp tools disable`
   - Контекст минимизируется

---

## ✅ Проверка соответствия архитектуры

### TC-1: Ограничение Cursor 2.x
**Статус**: ✅ РЕШЕНО
- Используется Docker MCP Toolkit Gateway с предварительным подключением всех серверов
- Управление видимостью tools через enable/disable
- Перезапуск соединения НЕ требуется

### TC-2: MCP Protocol ограничения
**Статус**: ✅ РЕШЕНО
- Gateway предоставляет единый список tools при подключении
- Видимость tools управляется через enable/disable
- MCP Protocol работает в стандартном режиме

### TC-3: Docker MCP Toolkit CLI
**Статус**: ✅ РЕШЕНО
- Используются команды `docker mcp tools enable/disable`
- JSON формат вывода для парсинга
- Retry-логика для надежности

### TC-4: Token Budget
**Статус**: ✅ РЕШЕНО
- Orchestrator предоставляет только 6 tools
- Tools от других серверов включаются только по требованию
- Метаданные вместо полных tools для выбора

---

## 🎯 Уточненная архитектура

### Изменения в архитектуре:

1. **Docker MCP Toolkit Gateway** должен быть запущен с конфигурацией:
   ```yaml
   # Все серверы из Catalog подключены
   servers: ["github-mcp", "postgres-mcp", "filesystem-mcp", ...]
   
   # Но tools отключены по умолчанию
   # (или включены только базовые tools от Orchestrator)
   ```

2. **Orchestrator** управляет видимостью tools:
   - `start_servers(["github-mcp"])` → включает все tools от github-mcp
   - `stop_servers(["github-mcp"])` → отключает все tools от github-mcp
   - Можно также управлять отдельными tools для более тонкой настройки

3. **AI Prompt Rules** обновлены:
   - AI не нужно беспокоиться о перезапуске соединения
   - Tools становятся доступны сразу после активации
   - Можно динамически включать/выключать tools

---

## 🔧 Технические детали реализации

### Команды для управления tools:

```bash
# Включить все tools от сервера github-mcp
docker mcp tools enable --server github-mcp

# Или включить конкретные tools
docker mcp tools enable github_create_repo github_list_issues --server github-mcp

# Отключить все tools от сервера
docker mcp tools disable --server github-mcp

# Получить список активных tools
docker mcp tools ls --format=json
```

### Определение tools по серверу:

При получении списка tools через `docker mcp tools ls`, нужно определить, какой tool принадлежит какому серверу. Это можно сделать через:
- Метаданные из Catalog
- Префиксы в именах tools (если есть соглашение)
- Информацию из `docker mcp server inspect`

---

## 📊 Итоговая оценка

### Соответствие требованиям: ✅ 100%

- ✅ Zero-Configuration для пользователя
- ✅ Оптимизация контекста через управление tools
- ✅ Прозрачность работы системы
- ✅ Масштабируемость
- ✅ Надежность
- ✅ Производительность

### Соответствие ограничениям: ✅ 100%

- ✅ Обход ограничения Cursor 2.x через gateway
- ✅ Соответствие MCP Protocol
- ✅ Использование Docker MCP Toolkit CLI
- ✅ Минимизация токенов

### Готовность к реализации: ✅ ДА

Архитектура полностью соответствует возможностям и ограничениям всех компонентов системы.
