# Архитектура Docker MCP Orchestrator

## 📐 Обзор архитектуры

Docker MCP Orchestrator — это интеллектуальная система управления MCP серверами, которая работает как промежуточный слой между AI-клиентом (Cursor) и множеством MCP серверов, обеспечивая динамическое управление контекстом и оптимизацию использования tools.

---

## 🏗️ Компоненты системы

### 1. Orchestrator MCP Server (Основной компонент)

**Назначение**: Главный MCP сервер, который подключается к Cursor и предоставляет минимальный набор tools для управления другими MCP серверами.

**Функции**:
- Предоставляет API для получения списка доступных серверов
- Управляет жизненным циклом MCP серверов через Docker MCP Toolkit
- Отслеживает состояние серверов
- Предоставляет метаданные о серверах и их tools

**Tools, предоставляемые Orchestrator**:
1. `list_available_servers` - получить список доступных серверов из Catalog с описаниями
2. `start_servers` - запустить указанные серверы через Docker MCP Toolkit
3. `stop_servers` - остановить указанные серверы
4. `get_active_servers` - получить список активных серверов
5. `get_server_tools` - получить список tools от указанного сервера (метаданные)
6. `get_server_info` - получить детальную информацию о сервере

**Важно**: Orchestrator предоставляет **только эти 6 tools**, что минимизирует контекст.

---

### 2. Docker MCP Toolkit Integration Layer

**Назначение**: Слой интеграции с Docker MCP Toolkit для управления серверами.

**Функции**:
- Выполнение CLI команд `docker mcp`
- Парсинг вывода команд
- Управление состоянием серверов (enable/disable)
- Получение информации о серверах из Catalog

**Используемые команды**:
- `docker mcp catalog show [name] --format=json` - получение списка серверов из Catalog
- `docker mcp server ls --json` - список активных серверов
- `docker mcp server enable <server1> <server2> ...` - активация серверов
- `docker mcp server disable <server1> <server2> ...` - деактивация серверов
- `docker mcp tools ls --format=json` - список tools от активных серверов
- `docker mcp server inspect <server>` - детальная информация о сервере

---

### 3. Server Registry & Metadata Cache

**Назначение**: Хранилище метаданных о доступных серверах и их tools.

**Функции**:
- Кэширование списка доступных серверов из Catalog
- Хранение метаданных о tools каждого сервера
- Индексация серверов по ключевым словам и функциональности
- Обновление метаданных при изменении Catalog

**Структура данных**:
```json
{
  "servers": {
    "github-mcp": {
      "name": "github-mcp",
      "description": "GitHub integration for repositories, issues, PRs",
      "keywords": ["github", "git", "repository", "pr", "issue"],
      "tools_count": 45,
      "tools_preview": ["create_repository", "list_issues", "create_pr", ...],
      "catalog_source": "docker-mcp"
    },
    ...
  }
}
```

---

### 4. Workflow Orchestration Engine

**Назначение**: Логика определения необходимых серверов и управления их жизненным циклом.

**Функции**:
- Анализ запроса AI для определения релевантных серверов
- Приоритизация серверов на основе контекста задачи
- Управление последовательностью запуска/остановки серверов
- Обработка зависимостей между серверами

**Алгоритм выбора серверов**:
1. AI запрашивает список доступных серверов через `list_available_servers`
2. AI анализирует задачу и выбирает нужные серверы
3. AI запрашивает запуск серверов через `start_servers`
4. Orchestrator активирует серверы через Docker MCP Toolkit
5. AI получает подтверждение и информацию о доступных tools

---

## 🔄 Архитектурные паттерны и решения

### Паттерн 1: Proxy Gateway Pattern

**Проблема**: Cursor требует перезапуска MCP соединения для обнаружения новых tools, но мы не можем динамически добавлять tools.

**Решение**: 
- Orchestrator работает как **мета-сервер** с минимальным набором tools
- Физические MCP серверы управляются через Docker MCP Toolkit **вне** основного соединения Cursor
- После активации серверов через Docker MCP Toolkit, они становятся доступны Cursor через стандартный механизм Docker MCP Toolkit

**Важное уточнение**: 
Docker MCP Toolkit уже имеет механизм gateway (`docker mcp gateway run`), который может управлять множеством серверов. Orchestrator использует этот механизм, но добавляет интеллектуальный слой управления.

---

### Паттерн 2: Lazy Loading & On-Demand Activation

**Проблема**: Нельзя загружать все серверы сразу из-за ограничений токенов.

**Решение**:
- Серверы активируются только по запросу AI
- AI получает метаданные о tools без активации серверов
- Активация происходит только когда AI явно запрашивает сервер

**Workflow**:
```
1. AI: "Мне нужны инструменты для работы с GitHub"
2. Orchestrator: "Доступен сервер github-mcp с 45 tools: [список]"
3. AI: "Запусти github-mcp"
4. Orchestrator: Активирует сервер через `docker mcp server enable github-mcp`
5. AI: Теперь может использовать tools от github-mcp (через Docker MCP Toolkit gateway)
```

---

### Паттерн 3: Metadata-First Approach

**Проблема**: AI нужно знать, какие tools доступны, но активация всех серверов перегружает контекст.

**Решение**:
- Orchestrator предоставляет **метаданные** о tools без активации серверов
- Метаданные хранятся в кэше и обновляются из Catalog
- AI может просматривать доступные tools через метаданные
- Активация происходит только для выбранных серверов

**Преимущества**:
- Минимальный контекст (только метаданные)
- Быстрый поиск нужных серверов
- Нет необходимости активировать серверы для просмотра их возможностей

---

## 🔌 Интеграция с Cursor и Docker MCP Toolkit

### Схема подключения

```
┌─────────────┐
│   Cursor    │
│  (AI Client)│
└──────┬──────┘
       │ MCP Protocol (stdio)
       │
       ▼
┌─────────────────────────────┐
│  Orchestrator MCP Server    │
│  (docker-mcp-orchestrator)  │
│                             │
│  Tools:                     │
│  - list_available_servers   │
│  - start_servers            │
│  - stop_servers             │
│  - get_active_servers       │
│  - get_server_tools         │
│  - get_server_info          │
└──────┬──────────────────────┘
       │ CLI Commands
       │ docker mcp ...
       ▼
┌─────────────────────────────┐
│  Docker MCP Toolkit         │
│  (Gateway Mode)             │
│                             │
│  Manages:                   │
│  - github-mcp               │
│  - postgres-mcp             │
│  - filesystem-mcp           │
│  - ...                      │
└─────────────────────────────┘
```

### Важное замечание о Cursor 2.x

**Ограничение**: Cursor 2.x требует перезапуска MCP соединения для обнаружения новых tools.

**Решение**:
1. Orchestrator активирует серверы через Docker MCP Toolkit
2. Docker MCP Toolkit gateway автоматически делает tools доступными
3. **НО**: Cursor все равно требует перезапуска соединения для обнаружения новых tools

**Альтернативное решение** (если перезапуск нежелателен):
- Использовать Docker MCP Toolkit gateway в режиме, где все серверы подключены, но tools фильтруются
- Orchestrator управляет **активностью** tools через `docker mcp tools enable/disable`
- Это позволяет динамически включать/выключать tools без перезапуска соединения

**Рекомендуемый подход**:
Использовать комбинацию:
1. Управление серверами через `docker mcp server enable/disable` (для полной активации/деактивации)
2. Управление tools через `docker mcp tools enable/disable` (для тонкой настройки)

---

## 🤖 AI Prompt Rules для работы с Orchestrator

### Правила для AI (добавить в .cursorrules или аналогичный файл)

```markdown
## Docker MCP Orchestrator Workflow

При работе с задачами, требующими использования MCP серверов:

1. **Всегда начинай с получения списка доступных серверов**:
   - Используй tool `list_available_servers` для получения списка всех доступных MCP серверов
   - Изучи описания и ключевые слова серверов

2. **Анализируй задачу и выбирай релевантные серверы**:
   - На основе описания задачи определи, какие серверы могут быть полезны
   - Используй метаданные о tools через `get_server_tools` для уточнения возможностей

3. **Запускай только необходимые серверы**:
   - Используй `start_servers` для активации выбранных серверов
   - НЕ запускай все серверы сразу - это перегрузит контекст
   - Запускай серверы по мере необходимости

4. **После запуска серверов**:
   - Получи список активных серверов через `get_active_servers`
   - Убедись, что нужные tools доступны
   - Если tools не появились, может потребоваться перезапуск MCP соединения (сообщи пользователю)

5. **Останавливай неиспользуемые серверы**:
   - После завершения задачи используй `stop_servers` для освобождения ресурсов
   - Это помогает минимизировать контекст для следующих задач

6. **Оптимизация**:
   - Если задача требует работы с несколькими серверами, запускай их последовательно или батчами
   - Избегай одновременной активации большого количества серверов (>5)
```

---

## 📊 Потоки данных

### Поток 1: Получение списка доступных серверов

```
AI Request
    │
    ▼
Orchestrator.list_available_servers()
    │
    ├─► Проверка кэша
    │   │
    │   ├─► Кэш актуален → возврат из кэша
    │   │
    │   └─► Кэш устарел → обновление
    │
    └─► docker mcp catalog show --format=json
        │
        └─► Парсинг JSON
            │
            └─► Обогащение метаданными
                │
                └─► Возврат AI
```

### Поток 2: Запуск серверов

```
AI Request: start_servers(["github-mcp", "postgres-mcp"])
    │
    ▼
Orchestrator.start_servers()
    │
    ├─► Валидация списка серверов
    │   │
    │   └─► Проверка существования в Catalog
    │
    ├─► Проверка текущего состояния
    │   │
    │   └─► docker mcp server ls --json
    │
    ├─► Активация серверов
    │   │
    │   └─► docker mcp server enable github-mcp postgres-mcp
    │
    ├─► Ожидание готовности (polling)
    │   │
    │   └─► docker mcp server inspect <server>
    │
    └─► Получение списка tools
        │
        └─► docker mcp tools ls --format=json
            │
            └─► Фильтрация по серверам
                │
                └─► Возврат AI
```

### Поток 3: Использование tools от активированных серверов

```
AI использует tool от активированного сервера
    │
    ▼
Docker MCP Toolkit Gateway
    │
    ├─► Маршрутизация к нужному серверу
    │
    └─► Выполнение tool
        │
        └─► Возврат результата AI
```

---

## 🔒 Обработка ошибок и надежность

### Стратегии обработки ошибок

1. **Ошибки активации сервера**:
   - Retry с экспоненциальной задержкой (3 попытки)
   - Логирование ошибки
   - Graceful degradation - продолжение работы с доступными серверами

2. **Недоступность Docker MCP Toolkit**:
   - Проверка доступности перед выполнением команд
   - Информативное сообщение об ошибке
   - Fallback на кэшированные метаданные

3. **Таймауты**:
   - Таймаут на активацию сервера: 10 секунд
   - Таймаут на получение списка tools: 5 секунд
   - Асинхронная обработка длительных операций

4. **Состояние серверов**:
   - Периодическая проверка состояния активных серверов
   - Автоматическое восстановление при сбоях
   - Уведомление о проблемах

---

## 🚀 Производительность

### Оптимизации

1. **Кэширование метаданных**:
   - Кэш списка серверов: TTL 5 минут
   - Кэш метаданных tools: TTL 10 минут
   - Инвалидация при изменении Catalog

2. **Батчинг операций**:
   - Группировка команд активации/деактивации
   - Параллельное выполнение независимых операций

3. **Lazy loading**:
   - Загрузка метаданных только при запросе
   - Отложенная активация серверов

4. **Минимизация контекста**:
   - Orchestrator предоставляет только 6 tools
   - Метаданные о tools в сжатом формате
   - Краткие описания серверов

---

## 📝 Конфигурация

### Структура конфигурации

```yaml
orchestrator:
  # Настройки кэширования
  cache:
    servers_ttl: 300  # 5 минут
    tools_ttl: 600    # 10 минут
  
  # Настройки Docker MCP Toolkit
  docker_mcp:
    catalog: "docker-mcp"  # Имя каталога по умолчанию
    gateway_mode: true     # Использовать gateway mode
    
  # Настройки производительности
  performance:
    server_start_timeout: 10  # секунд
    tools_fetch_timeout: 5    # секунд
    max_concurrent_servers: 5
    
  # Настройки надежности
  reliability:
    retry_attempts: 3
    retry_delay: 1  # секунд (экспоненциальная задержка)
```

---

## ✅ Соответствие требованиям

### BR-1: Zero-Configuration
✅ Пользователь один раз настраивает список серверов в Docker MCP Catalog
✅ Orchestrator автоматически управляет серверами

### BR-2: Оптимизация контекста
✅ Orchestrator предоставляет только 6 tools
✅ Серверы активируются по требованию
✅ Метаданные вместо полных tools

### BR-3: Прозрачность
✅ AI может получить список активных серверов
✅ Логирование всех операций
✅ Метаданные о tools доступны

### BR-4: Масштабируемость
✅ Работа с любым количеством серверов из Catalog
✅ Кэширование для производительности
✅ Динамическое добавление серверов

### BR-5: Надежность
✅ Обработка ошибок и retry
✅ Graceful degradation
✅ Логирование

### BR-6: Производительность
✅ Кэширование метаданных
✅ Батчинг операций
✅ Асинхронная обработка

---

## 🔍 Проверка соответствия ограничениям

### TC-1: Ограничение Cursor 2.x
✅ Решение: Использование Docker MCP Toolkit gateway + управление tools через enable/disable
⚠️ Примечание: Может потребоваться перезапуск соединения для полной активации серверов

### TC-2: MCP Protocol ограничения
✅ Решение: Orchestrator работает как мета-сервер, физические серверы управляются через Docker MCP Toolkit

### TC-3: Docker MCP Toolkit CLI
✅ Решение: Надежный парсинг JSON вывода команд, retry-логика, обработка ошибок

### TC-4: Token Budget
✅ Решение: Минимальный набор tools (6), метаданные вместо полных tools, активация по требованию

---

## 🎯 Следующие шаги

1. ✅ Архитектура определена
2. ⏳ Реализация Orchestrator MCP Server
3. ⏳ Интеграция с Docker MCP Toolkit
4. ⏳ Реализация кэширования метаданных
5. ⏳ Создание AI Prompt Rules
6. ⏳ Тестирование и оптимизация
