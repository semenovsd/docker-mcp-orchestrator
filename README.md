# Docker MCP Orchestrator

## 🎯 Описание

Docker MCP Orchestrator — это интеллектуальная система управления MCP серверами, которая работает как proxy/gateway между AI-клиентом (Cursor) и множеством MCP серверов. Orchestrator предоставляет инструменты для управления серверами и проксирует все взаимодействие с запущенными серверами.

### Ключевые возможности

- ✅ **Управление MCP серверами**: Запуск, остановка, получение информации о серверах
- ✅ **Proxy функциональность**: Все взаимодействие с запущенными серверами идет через Orchestrator
- ✅ **Управление конфигурацией**: Настройка серверов, управление секретами
- ✅ **Управление промптами**: Автоматическое извлечение и предоставление промптов для серверов
- ✅ **Кэширование**: Эффективное кэширование метаданных для производительности
- ✅ **14 Tools**: Полный набор инструментов для управления MCP серверами

---

## 📋 Требования

- Python 3.11+
- Docker Desktop с установленным MCP Toolkit
- MCP серверы, добавленные в Docker MCP Catalog

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd docker-mcp-orchestrator
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или используя pip в режиме разработки:

```bash
pip install -e .
```

### 3. Настройка конфигурации

Конфигурация находится в `config/config.yaml`. Вы можете настроить:

- TTL для кэширования
- Таймауты для команд
- Настройки proxy и connection pool
- Уровень логирования

### 4. Добавление в Cursor

Добавьте Orchestrator в конфигурацию Cursor MCP серверов:

**Для Cursor** (`.cursor/mcp.json` или аналогичный файл):

```json
{
  "mcpServers": {
    "docker-mcp-orchestrator": {
      "command": "python",
      "args": ["-m", "orchestrator"],
      "cwd": "/path/to/docker-mcp-orchestrator"
    }
  }
}
```

Или используя абсолютный путь:

```json
{
  "mcpServers": {
    "docker-mcp-orchestrator": {
      "command": "python",
      "args": ["/absolute/path/to/docker-mcp-orchestrator/src/orchestrator/__main__.py"]
    }
  }
}
```

---

## 📚 Использование

### Workflow работы с Orchestrator

1. **Получи список установленных серверов**:
   ```
   list_installed_servers()
   ```

2. **Изучи возможности сервера**:
   ```
   get_server_tools(server="github-mcp")
   get_server_info(server="github-mcp")
   ```

3. **Настрой конфигурацию** (если необходимо):
   ```
   secret_set(key="GITHUB_TOKEN", value="your-token")
   config_set(server="postgres-mcp", config={"DATABASE_URL": "postgres://..."})
   ```

4. **Запусти нужные серверы**:
   ```
   start_servers(servers=["github-mcp", "postgres-mcp"])
   ```
   
   После запуска вы получите:
   - Список доступных tools
   - Промпты для добавления в контекст (если есть)

5. **Используй tools через proxy**:
   ```
   call_tool(tool_name="github_create_repo", arguments={"name": "my-repo"})
   ```

6. **Останови серверы после завершения**:
   ```
   stop_servers(servers=["github-mcp", "postgres-mcp"])
   ```

### Доступные Tools

#### Управление серверами (5 tools)
- `list_installed_servers` - Список установленных серверов
- `list_catalog_servers` - Список серверов в каталоге
- `start_servers` - Запуск серверов
- `stop_servers` - Остановка серверов
- `get_active_servers` - Список активных серверов

#### Информация о серверах (2 tools)
- `get_server_tools` - Метаданные о tools сервера
- `get_server_info` - Детальная информация о сервере

#### Управление конфигурацией (5 tools)
- `config_set` - Установка конфигурации
- `config_get` - Получение конфигурации
- `secret_set` - Установка секрета
- `secret_list` - Список секретов
- `secret_remove` - Удаление секрета

#### Proxy (2 tools)
- `call_tool` - Вызов tool через proxy
- `list_active_tools` - Список активных tools

---

## 🤖 AI Prompt Rules

Для правильной работы с Orchestrator добавьте правила в `.cursorrules` или аналогичный файл:

```markdown
## Docker MCP Orchestrator Workflow

При работе с MCP серверами:

1. Всегда начинай с list_installed_servers()
2. Изучи возможности через get_server_tools() и get_server_info()
3. Настрой конфигурацию перед запуском (secret_set, config_set)
4. Запусти серверы через start_servers()
5. Добавь промпты в контекст (если предоставлены)
6. Используй tools через call_tool() (НЕ напрямую!)
7. Останови серверы через stop_servers() после завершения
```

Полные правила находятся в `prompts/ai_rules.md`.

---

## 🏗️ Архитектура

### Компоненты

1. **Orchestrator MCP Server** - Основной сервер с 14 tools
2. **Docker MCP Client** - Интеграция с Docker MCP Toolkit
3. **Connection Pool** - Управление соединениями с MCP серверами
4. **Proxy Layer** - Маршрутизация запросов к серверам
5. **Cache Manager** - Кэширование метаданных
6. **Prompt Manager** - Управление промптами

### Схема взаимодействия

```
Cursor → Orchestrator MCP Server (14 tools)
              │
              ├─► Docker MCP Toolkit CLI
              │
              └─► Proxy Layer
                  ├─► Connection Pool
                  │   ├─► github-mcp
                  │   ├─► postgres-mcp
                  │   └─► ...
                  │
                  └─► Tool Router
```

---

## 📝 Конфигурация

Конфигурация находится в `config/config.yaml`:

```yaml
orchestrator:
  cache:
    servers_ttl: 300  # 5 минут
    tools_ttl: 600    # 10 минут
  
  docker_mcp:
    catalog: "docker-mcp"
    command_timeout: 30
    
  proxy:
    connection_pool_size: 10
    connection_timeout: 30
    reconnect_attempts: 3
```

---

## 🔧 Разработка

### Структура проекта

```
docker-mcp-orchestrator/
├── src/
│   └── orchestrator/
│       ├── __init__.py
│       ├── server.py              # Основной MCP сервер
│       ├── docker_client.py       # Docker MCP Toolkit клиент
│       ├── cache.py                # Кэширование
│       ├── connection_pool.py      # Пул соединений
│       ├── proxy.py                # Proxy layer
│       ├── prompt_manager.py       # Управление промптами
│       ├── models.py               # Модели данных
│       ├── utils.py                # Утилиты
│       └── tools/                  # Все tools
├── config/
│   └── config.yaml                 # Конфигурация
├── prompts/
│   └── ai_rules.md                 # AI правила
├── tests/                           # Тесты
├── README.md
└── requirements.txt
```

### Запуск в режиме разработки

```bash
python -m orchestrator
```

### Тестирование

```bash
pytest tests/
```

---

## ⚠️ Важные замечания

### Connection Pool

**Важно**: Механизм подключения к запущенным MCP серверам требует уточнения. В текущей реализации `connection_pool.py` содержит placeholder, который нужно будет реализовать после определения того, как Docker MCP Toolkit предоставляет доступ к запущенным серверам.

Варианты реализации:
1. Через Docker MCP Toolkit gateway API
2. Прямое подключение к контейнерам через stdio
3. Использование SSE/streaming transport

### Proxy функциональность

Все взаимодействие с запущенными серверами идет через Orchestrator proxy. AI должен использовать `call_tool()` для вызова tools, а не пытаться вызывать их напрямую.

---

## 📚 Документация

- [ARCHITECTURE_V2.md](./ARCHITECTURE_V2.md) - Детальная архитектура
- [IMPLEMENTATION_PLAN_V2.md](./IMPLEMENTATION_PLAN_V2.md) - План реализации
- [prompts/ai_rules.md](./prompts/ai_rules.md) - AI правила для работы

---

## 🐛 Известные проблемы

1. **Connection Pool**: Требуется реализация механизма подключения к серверам
2. **MCP API**: Формат возвращаемых значений может потребовать уточнения в зависимости от версии MCP библиотеки

---

## 📄 Лицензия

[Указать лицензию]

---

## 🤝 Вклад

[Инструкции по вкладу]

---

## 📞 Поддержка

[Контактная информация]
